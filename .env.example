# ========================================
# DEVELOPMENT ENVIRONMENT (profile: dev)
# ========================================

# Docker Compose - PostgreSQL Configuration
POSTGRES_DB=knowledge_tracker_dev
POSTGRES_USER=pragma_dev
POSTGRES_PASSWORD=pragma_dev_password
POSTGRES_PORT=5432

# Docker Compose - Elasticsearch Configuration
ES_PORT=9200
ES_PASSWORD=elasticsearch
ES_HEAP_SIZE=512m

# Docker Compose - Logstash Configuration
LOGSTASH_PORT=5000
LOGSTASH_HEAP_SIZE=256m

# Docker Compose - Kibana Configuration (used with --with-kibana)
KIBANA_PORT=5601

# Docker Compose - pgAdmin Configuration (used with --with-pgadmin)
PGADMIN_EMAIL=admin@pragma.com
PGADMIN_PASSWORD=admin
PGADMIN_PORT=5050

# Application - Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=knowledge_tracker_dev
DB_USERNAME=pragma_dev
DB_PASSWORD=pragma_dev_password

# Connection Pool Settings
DB_POOL_SIZE=5
DB_MIN_IDLE=2
DB_CONNECTION_TIMEOUT=30000

# Server Configuration
SERVER_PORT=8080

# Elasticsearch Configuration
ES_HOST=localhost
ES_PORT_APP=9200
ES_USERNAME=elastic
ES_PASSWORD_APP=elasticsearch

# ========================================
# TEST ENVIRONMENT (profile: test)
# ========================================

# Database Configuration
TEST_DB_HOST=localhost
TEST_DB_PORT=5432
TEST_DB_NAME=knowledge_tracker_test
TEST_DB_USERNAME=postgres
TEST_DB_PASSWORD=postgres

# Connection Pool Settings
TEST_DB_POOL_SIZE=5
TEST_DB_MIN_IDLE=2
TEST_DB_CONNECTION_TIMEOUT=30000

# Server Configuration
TEST_SERVER_PORT=8081

# ========================================
# PRODUCTION ENVIRONMENT (profile: prod)
# ========================================
# IMPORTANT: Change all production values before deploying!

# Database Configuration
PROD_DB_HOST=localhost
PROD_DB_PORT=5432
PROD_DB_NAME=knowledge_tracker
PROD_DB_USERNAME=postgres
PROD_DB_PASSWORD=changeme

# Connection Pool Settings (Production-optimized)
PROD_DB_POOL_SIZE=20
PROD_DB_MIN_IDLE=5
PROD_DB_CONNECTION_TIMEOUT=30000
PROD_DB_IDLE_TIMEOUT=600000
PROD_DB_MAX_LIFETIME=1800000
PROD_DB_LEAK_DETECTION_THRESHOLD=60000

# Server Configuration
PROD_SERVER_PORT=8080